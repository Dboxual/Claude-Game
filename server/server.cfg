# ============================================================
#  TacMove dedicated server configuration
#  key = value; '#' starts a comment (so names cannot contain #).
# ============================================================

server_name = My TacMove Server
max_players = 16      # 1..128
tick_rate   = 128     # simulation ticks per second (10..512)
